%p#notice= notice
%h1 User Lessons
%table
  %thead
    %tr
      %th Score
      %th User
      %th Lesson
      %th{:colspan => "3"}
  %tbody
    - @user_lessons.each do |user_lesson|
      %tr
        %td= user_lesson.score
        %td= user_lesson.user.name
        %td= user_lesson.lesson.name
        %td= link_to 'Show', user_lesson
        %td= link_to 'Edit', edit_user_lesson_path(user_lesson)
        %td= link_to 'Destroy', user_lesson, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New User Lesson', new_user_lesson_path
